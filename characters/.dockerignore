# 1) Imagen base de Python
FROM python:3.10-slim

# 2) Para ver bien los logs en la consola
ENV PYTHONUNBUFFERED=1

# 3) Carpeta de trabajo dentro del contenedor
WORKDIR /app

# 4) Copiar dependencias e instalarlas
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 5) Copiar TODO el proyecto
COPY . .

# 6) Exponer puerto donde correrá Django
EXPOSE 8000

# 7) Comando para arrancar Django
CMD ["python", "manage.py", "runserver", "0.0.0.0:8000"]
